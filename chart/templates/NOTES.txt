1. Check the deployment:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "sre-agent.name" . }},app.kubernetes.io/instance={{ .Release.Name }}

2. Access the web UI locally:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "sre-agent.fullname" . }} 8080:{{ .Values.service.port }}
   Open http://localhost:8080

3. Confirm runtime health:
   curl -s http://localhost:8080/health

{{- if and (not .Values.existingSecret) (not .Values.secrets.create) }}
NOTICE: No credential Secret is configured. The pod can start, but model-backed
requests require either existingSecret or secrets.create=true.
{{- end }}

{{- if .Values.rbac.write.create }}
WARNING: Cluster-wide writer RBAC is enabled. Confirm the approval surface is
authenticated and SLACK_APPROVER_IDS is restricted before production use.
{{- else }}
Writer RBAC is disabled; the agent can inspect the cluster but cannot remediate it.
{{- end }}
